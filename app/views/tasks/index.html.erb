<h1>My Tasks</h1>
<ul class="container">
  <% @tasks.each do |task| %>
    <i class="far fa-square"></i><li class="list"><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<%= link_to "Add a new task", tasks_new_path %>
